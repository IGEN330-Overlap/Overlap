import './Navbar.css';

const Navbar = (props) => {
    return (
        <nav class="navbar fixed-top navbar-light bg-light">
            <span class="navbar-text">
                Overlap
            </span>
        </nav>
    );
}

export default Navbar;
